mean_velocity_2d(bezier_path_x, bezier_path_y),
mean_velocity_2d(space_path_x, space_path_y),
mean_velocity_2d(heart_path_x,heart_path_y)
),
Total_Distance = c(
total_distance_2d(bezier_path_x, bezier_path_y),
total_distance_2d(space_path_x, space_path_y),
total_distance_2d(heart_path_x, heart_path_y)
),
Mean_Abs_Direction_Change = c(
mean_abs_change_in_direction(bezier_path_x,bezier_path_y),
mean_abs_change_in_direction(space_path_x, space_path_y),
mean_abs_change_in_direction(heart_path_x, heart_path_y)
),
P80_Acceleration = c(
p80_percent_acceleration(bezier_path_x, bezier_path_y),
p80_percent_acceleration(space_path_x, space_path_y),
p80_percent_acceleration(heart_path_x, heart_path_y)
)
)
format_signif_table <- function(df, digits = 4) {
df[] <- lapply(df, function(col) {
if (is.numeric(col)) signif(col, digits) else col
})
return(df)
}
formatted_results = format_signif_table(results, digits = 4)
print(results)
mean_velocity_2d = function(x,y){
dx = diff(x)
dy = diff(y)
v = sqrt(dx^2 + dy^2)
m_v = mean(v)
return(m_v)
}
px = c(0,3,7,10)
py = c(0,5,5,2)
t = seq(from=0, to=1, by=0.01)
bezier_path_x = (1-t)^3*px[1] + 3*(1-t)^2*t*px[2] + 3*(1-t)*t^2*px[3] + t^3*px[4]
bezier_path_y = (1-t)^3*py[1] + 3*(1-t)^2*t*py[2] + 3*(1-t)*t^2*py[3] + t^3*py[4]
t = seq(from=0, to=2*pi, length=101)
space_path_x = c((t + 4)*sin(3*t), seq(from=0, to=30, length=40), rev(30 - (t + 1)*sin(3*t)))
space_path_y = c((t + 4)*cos(3*t), seq(from=10.283, to=19.283, length=40), rev(12 + (t + 1)*cos(3*t)))
t = seq(from=0, to=2*pi, length=101)
heart_path_x = 16*sin(t)^3
heart_path_y = 13*cos(t) - 5*cos(2*t) - 2*cos(3*t) - cos(4*t)
results = data.frame(
test_cases = c("Mean Velocity", "Total Distance", "Mean Abs Direction Change", "80th Percentile Acceleration"),
Bezier = c(
mean_velocity_2d(bezier_path_x, bezier_path_y),
total_distance_2d(bezier_path_x, bezier_path_y),
mean_abs_change_in_direction(bezier_path_x,bezier_path_y),
p80_percent_acceleration(bezier_path_x, bezier_path_y)
),
Space = c(
mean_velocity_2d(space_path_x, space_path_y),
total_distance_2d(space_path_x, space_path_y),
mean_abs_change_in_direction(space_path_x, space_path_y),
p80_percent_acceleration(space_path_x, space_path_y)
),
Heart = c(
mean_velocity_2d(heart_path_x,heart_path_y),
total_distance_2d(heart_path_x, heart_path_y),
mean_abs_change_in_direction(heart_path_x, heart_path_y),
p80_percent_acceleration(heart_path_x, heart_path_y)
)
)
format_signif_df <- function(df, digits = 4) {
df[-1] <- lapply(df[-1], function(col) signif(col, digits))
return(df)
}
formatted_results = format_signif_table(results)
print(formatted_results)
install.packages("tinytex")
install.packages("tinytex")
install.packages("tinytex")
tinytex::install_tinytex()
install.packages("tinytex")
knitr::opts_chunk$set(echo = TRUE)
mean_velocity_2d = function(x,y){
dx = diff(x)
dy = diff(y)
v = sqrt(dx^2 + dy^2)
m_v = mean(v)
return(m_v)
}
px = c(0,3,7,10)
py = c(0,5,5,2)
t = seq(from=0, to=1, by=0.01)
bezier_path_x = (1-t)^3*px[1] + 3*(1-t)^2*t*px[2] + 3*(1-t)*t^2*px[3] + t^3*px[4]
bezier_path_y = (1-t)^3*py[1] + 3*(1-t)^2*t*py[2] + 3*(1-t)*t^2*py[3] + t^3*py[4]
t = seq(from=0, to=2*pi, length=101)
space_path_x = c((t + 4)*sin(3*t), seq(from=0, to=30, length=40), rev(30 - (t + 1)*sin(3*t)))
space_path_y = c((t + 4)*cos(3*t), seq(from=10.283, to=19.283, length=40), rev(12 + (t + 1)*cos(3*t)))
t = seq(from=0, to=2*pi, length=101)
heart_path_x = 16*sin(t)^3
heart_path_y = 13*cos(t) - 5*cos(2*t) - 2*cos(3*t) - cos(4*t)
results = data.frame(
test_cases = c("Mean Velocity", "Total Distance", "Mean Abs Direction Change", "80th Percentile Acceleration"),
Bezier = c(
mean_velocity_2d(bezier_path_x, bezier_path_y),
total_distance_2d(bezier_path_x, bezier_path_y),
mean_abs_change_in_direction(bezier_path_x,bezier_path_y),
p80_percent_acceleration(bezier_path_x, bezier_path_y)
),
Space = c(
mean_velocity_2d(space_path_x, space_path_y),
total_distance_2d(space_path_x, space_path_y),
mean_abs_change_in_direction(space_path_x, space_path_y),
p80_percent_acceleration(space_path_x, space_path_y)
),
Heart = c(
mean_velocity_2d(heart_path_x,heart_path_y),
total_distance_2d(heart_path_x, heart_path_y),
mean_abs_change_in_direction(heart_path_x, heart_path_y),
p80_percent_acceleration(heart_path_x, heart_path_y)
)
)
format_signif_df <- function(df, digits = 4) {
df[-1] <- lapply(df[-1], function(col) signif(col, digits))
return(df)
}
formatted_results = format_signif_table(results)
print(formatted_results)
mean_abs_change_in_direction = function(x, y) {
dx = diff(x)
dy = diff(y)
directions = atan2(dy, dx)
dtheta = diff(directions)
dtheta = (dtheta + pi) %% (2 * pi) - pi
mean_abs_change = mean(abs(dtheta))
}
knitr::opts_chunk$set(echo = TRUE)
mean_velocity_2d = function(x,y){
dx = diff(x)
dy = diff(y)
v = sqrt(dx^2 + dy^2)
m_v = mean(v)
return(m_v)
}
px = c(0,3,7,10)
py = c(0,5,5,2)
t = seq(from=0, to=1, by=0.01)
bezier_path_x = (1-t)^3*px[1] + 3*(1-t)^2*t*px[2] + 3*(1-t)*t^2*px[3] + t^3*px[4]
bezier_path_y = (1-t)^3*py[1] + 3*(1-t)^2*t*py[2] + 3*(1-t)*t^2*py[3] + t^3*py[4]
t = seq(from=0, to=2*pi, length=101)
space_path_x = c((t + 4)*sin(3*t), seq(from=0, to=30, length=40), rev(30 - (t + 1)*sin(3*t)))
space_path_y = c((t + 4)*cos(3*t), seq(from=10.283, to=19.283, length=40), rev(12 + (t + 1)*cos(3*t)))
t = seq(from=0, to=2*pi, length=101)
heart_path_x = 16*sin(t)^3
heart_path_y = 13*cos(t) - 5*cos(2*t) - 2*cos(3*t) - cos(4*t)
results = data.frame(
test_cases = c("Mean Velocity", "Total Distance", "Mean Abs Direction Change", "80th Percentile Acceleration"),
Bezier = c(
mean_velocity_2d(bezier_path_x, bezier_path_y),
total_distance_2d(bezier_path_x, bezier_path_y),
mean_abs_change_in_direction(bezier_path_x,bezier_path_y),
p80_percent_acceleration(bezier_path_x, bezier_path_y)
),
Space = c(
mean_velocity_2d(space_path_x, space_path_y),
total_distance_2d(space_path_x, space_path_y),
mean_abs_change_in_direction(space_path_x, space_path_y),
p80_percent_acceleration(space_path_x, space_path_y)
),
Heart = c(
mean_velocity_2d(heart_path_x,heart_path_y),
total_distance_2d(heart_path_x, heart_path_y),
mean_abs_change_in_direction(heart_path_x, heart_path_y),
p80_percent_acceleration(heart_path_x, heart_path_y)
)
)
format_signif_df <- function(df, digits = 4) {
df[-1] <- lapply(df[-1], function(col) signif(col, digits))
return(df)
}
formatted_results = format_signif_table(results)
print(formatted_results)
knitr::opts_chunk$set(echo = TRUE)
total_distance_2d = function(x, y) {
dx = diff(x)
dy = diff(y)
distances = sqrt(dx^2 + dy^2)
return(sum(distances))
}
mean_abs_change_in_direction = function(x, y) {
dx = diff(x)
dy = diff(y)
directions = atan2(dy, dx)
dtheta = diff(directions)
dtheta = (dtheta + pi) %% (2 * pi) - pi
mean_abs_change = mean(abs(dtheta))
}
max_abs_curvature_2d = function(x, y) {
dx = diff(x)
dy = diff(y)
ddx.= diff(dx)
ddy = diff(dy)
dx_adj = dx[-length(dx)]
dy_adj = dy[-length(dy)]
numerator = dx_adj * ddy - dy_adj * ddx
denominator = (dx_adj^2 + dy_mid^2)^(3/2)
curvature = numerator / denominator
curvature[is.nan(curvature)] = 0
return(max(abs(curvature), na.rm = TRUE))
}
p80_percent_acceleration = function(x, y){
dx = diff(x)
dy = diff(y)
ddx = diff(dx)
ddy = diff(dy)
acc = sqrt(ddx^2 + ddy^2)
acc_ordered = sort(acc)
index = round(length(acc_ordered) * 0.8)
if (index == 0) index = 1
return(acc_ordered[index])
}
mean_velocity_2d = function(x,y){
dx = diff(x)
dy = diff(y)
v = sqrt(dx^2 + dy^2)
m_v = mean(v)
return(m_v)
}
px = c(0,3,7,10)
py = c(0,5,5,2)
t = seq(from=0, to=1, by=0.01)
bezier_path_x = (1-t)^3*px[1] + 3*(1-t)^2*t*px[2] + 3*(1-t)*t^2*px[3] + t^3*px[4]
bezier_path_y = (1-t)^3*py[1] + 3*(1-t)^2*t*py[2] + 3*(1-t)*t^2*py[3] + t^3*py[4]
t = seq(from=0, to=2*pi, length=101)
space_path_x = c((t + 4)*sin(3*t), seq(from=0, to=30, length=40), rev(30 - (t + 1)*sin(3*t)))
space_path_y = c((t + 4)*cos(3*t), seq(from=10.283, to=19.283, length=40), rev(12 + (t + 1)*cos(3*t)))
t = seq(from=0, to=2*pi, length=101)
heart_path_x = 16*sin(t)^3
heart_path_y = 13*cos(t) - 5*cos(2*t) - 2*cos(3*t) - cos(4*t)
results = data.frame(
test_cases = c("Mean Velocity", "Total Distance", "Mean Abs Direction Change", "80th Percentile Acceleration"),
Bezier = c(
mean_velocity_2d(bezier_path_x, bezier_path_y),
total_distance_2d(bezier_path_x, bezier_path_y),
mean_abs_change_in_direction(bezier_path_x,bezier_path_y),
p80_percent_acceleration(bezier_path_x, bezier_path_y)
),
Space = c(
mean_velocity_2d(space_path_x, space_path_y),
total_distance_2d(space_path_x, space_path_y),
mean_abs_change_in_direction(space_path_x, space_path_y),
p80_percent_acceleration(space_path_x, space_path_y)
),
Heart = c(
mean_velocity_2d(heart_path_x,heart_path_y),
total_distance_2d(heart_path_x, heart_path_y),
mean_abs_change_in_direction(heart_path_x, heart_path_y),
p80_percent_acceleration(heart_path_x, heart_path_y)
)
)
format_signif_df <- function(df, digits = 4) {
df[-1] <- lapply(df[-1], function(col) signif(col, digits))
return(df)
}
formatted_results = format_signif_table(results)
print(formatted_results)
knitr::opts_chunk$set(echo = TRUE)
total_distance_2d = function(x, y) {
dx = diff(x)
dy = diff(y)
distances = sqrt(dx^2 + dy^2)
return(sum(distances))
}
mean_abs_change_in_direction = function(x, y) {
dx = diff(x)
dy = diff(y)
directions = atan2(dy, dx)
dtheta = diff(directions)
dtheta = (dtheta + pi) %% (2 * pi) - pi
mean_abs_change = mean(abs(dtheta))
}
max_abs_curvature_2d = function(x, y) {
dx = diff(x)
dy = diff(y)
ddx.= diff(dx)
ddy = diff(dy)
dx_adj = dx[-length(dx)]
dy_adj = dy[-length(dy)]
numerator = dx_adj * ddy - dy_adj * ddx
denominator = (dx_adj^2 + dy_mid^2)^(3/2)
curvature = numerator / denominator
curvature[is.nan(curvature)] = 0
return(max(abs(curvature), na.rm = TRUE))
}
p80_percent_acceleration = function(x, y){
dx = diff(x)
dy = diff(y)
ddx = diff(dx)
ddy = diff(dy)
acc = sqrt(ddx^2 + ddy^2)
acc_ordered = sort(acc)
index = round(length(acc_ordered) * 0.8)
if (index == 0) index = 1
return(acc_ordered[index])
}
mean_velocity_2d = function(x,y){
dx = diff(x)
dy = diff(y)
v = sqrt(dx^2 + dy^2)
m_v = mean(v)
return(m_v)
}
px = c(0,3,7,10)
py = c(0,5,5,2)
t = seq(from=0, to=1, by=0.01)
bezier_path_x = (1-t)^3*px[1] + 3*(1-t)^2*t*px[2] + 3*(1-t)*t^2*px[3] + t^3*px[4]
bezier_path_y = (1-t)^3*py[1] + 3*(1-t)^2*t*py[2] + 3*(1-t)*t^2*py[3] + t^3*py[4]
t = seq(from=0, to=2*pi, length=101)
space_path_x = c((t + 4)*sin(3*t), seq(from=0, to=30, length=40), rev(30 - (t + 1)*sin(3*t)))
space_path_y = c((t + 4)*cos(3*t), seq(from=10.283, to=19.283, length=40), rev(12 + (t + 1)*cos(3*t)))
t = seq(from=0, to=2*pi, length=101)
heart_path_x = 16*sin(t)^3
heart_path_y = 13*cos(t) - 5*cos(2*t) - 2*cos(3*t) - cos(4*t)
results = data.frame(
test_cases = c("Mean Velocity", "Total Distance", "Mean Abs Direction Change", "80th Percentile Acceleration"),
Bezier = c(
mean_velocity_2d(bezier_path_x, bezier_path_y),
total_distance_2d(bezier_path_x, bezier_path_y),
mean_abs_change_in_direction(bezier_path_x,bezier_path_y),
p80_percent_acceleration(bezier_path_x, bezier_path_y)
),
Space = c(
mean_velocity_2d(space_path_x, space_path_y),
total_distance_2d(space_path_x, space_path_y),
mean_abs_change_in_direction(space_path_x, space_path_y),
p80_percent_acceleration(space_path_x, space_path_y)
),
Heart = c(
mean_velocity_2d(heart_path_x,heart_path_y),
total_distance_2d(heart_path_x, heart_path_y),
mean_abs_change_in_direction(heart_path_x, heart_path_y),
p80_percent_acceleration(heart_path_x, heart_path_y)
)
)
format_signif_df <- function(df, digits = 4) {
df[-1] <- lapply(df[-1], function(col) signif(col, digits))
return(df)
}
formatted_results = format_signif_df(results)
print(formatted_results)
install.packages("cancensus")
library(cancensus)
knitr::opts_chunk$set(echo = TRUE)
all_regions = list_census_regions("CA21") # 2021 census
rural_on_regions = subset(all_regions,
level=="CSD" & # census subdivisions
PR_UID == 35 & # in Ontario
is.na(CMA_UID)) # not inside a metropolitan area
csd_list = list(CSD = rural_on_regions$region)
census_data = get_census(dataset = "CA21",
regions = csd_list,
level = "CSD")
install.packages("readr")
library(readr)
all_regions = list_census_regions("CA21") # 2021 census
rural_on_regions = subset(all_regions,
level=="CSD" & # census subdivisions
PR_UID == 35 & # in Ontario
is.na(CMA_UID)) # not inside a metropolitan area
csd_list = list(CSD = rural_on_regions$region)
census_data = get_census(dataset = "CA21",
regions = csd_list,
level = "CSD")
set_cancensus_api_key('CensusMapper_8f2843919778393ad76175db2790989e', install=TRUE)
all_regions = list_census_regions("CA21") # 2021 census
rural_on_regions = subset(all_regions,
level=="CSD" & # census subdivisions
PR_UID == 35 & # in Ontario
is.na(CMA_UID)) # not inside a metropolitan area
csd_list = list(CSD = rural_on_regions$region)
census_data = get_census(dataset = "CA21",
regions = csd_list,
level = "CSD")
all_regions = list_census_regions("CA21") # 2021 census
rural_on_regions = subset(all_regions,
level=="CSD" & # census subdivisions
PR_UID == 35 & # in Ontario
is.na(CMA_UID)) # not inside a metropolitan area
csd_list = list(CSD = rural_on_regions$region)
census_data = get_census(dataset = "CA21",
regions = csd_list,
level = "CSD")
all_regions = list_census_regions("CA21") # 2021 census
rural_on_regions = subset(all_regions,
level=="CSD" & # census subdivisions
PR_UID == 35 & # in Ontario
is.na(CMA_UID)) # not inside a metropolitan area
csd_list = list(CSD = rural_on_regions$region)
census_data = get_census(dataset = "CA21",
regions = csd_list,
level = "CSD")
names(census_data)[4] = "Area"
head(census_data)
head(census_data)
all_regions = list_census_regions("CA21") # 2021 census
rural_on_regions = subset(all_regions,
level=="CSD" & # census subdivisions
PR_UID == 35 & # in Ontario
is.na(CMA_UID)) # not inside a metropolitan area
csd_list = list(CSD = rural_on_regions$region)
census_data = get_census(dataset = "CA21",
regions = csd_list,
level = "CSD")
names(census_data)[4] = "Area"
head(census_data)
head(census_data)
all_regions = list_census_regions("CA21") # 2021 census
rural_on_regions = subset(all_regions,
level=="CSD" & # census subdivisions
PR_UID == 35 & # in Ontario
is.na(CMA_UID)) # not inside a metropolitan area
csd_list = list(CSD = rural_on_regions$region)
census_data = get_census(dataset = "CA21",
regions = csd_list,
level = "CSD")
names(census_data)[4] = "Area"
head(census_data)
rural_on_regions = subset(all_regions, level=="CSD" & PR_UID == 35 & is.na(CMA_UID))
View(census_data)
table(all_regions$level, all_regions$PR_UID)
View(all_regions)
View(all_regions)
all_regions = list_census_regions("CA21") # 2021 census
library(cancensus)
all_regions = list_census_regions("CA21") # 2021 census
all_regions = list_census_regions("CA21")
rural_on_regions = subset(all_regions, level=="CSD" & PR_UID == 35 & is.na(CMA_UID))
csd_list = list(CSD = rural_on_regions$region)
census_data = get_census(dataset = "CA21",
regions = csd_list,
level = "CSD")
names(census_data)[4] = "Area"
head(census_data)
all_regions = list_census_regions("CA21")
rural_on_regions = subset(all_regions, level=="CSD" & PR_UID == 35 & is.na(CMA_UID))
csd_list = list(CSD = rural_on_regions$region)
census_data = get_census(dataset = "CA21",
regions = csd_list,
level = "CSD")
names(census_data)[4] = "Area"
head(census_data)
all_regions = list_census_regions("CA21") # 2021 census
rural_on_regions = subset(all_regions,
level=="CSD" &  # census subdivisions
PR_UID == 35 & # in Ontario
is.na(CMA_UID)) # not inside a metropolitan area
csd_list = list(CSD = rural_on_regions$region)
census_data = get_census(dataset = "CA21",
regions = csd_list,
level = "CSD")
names(census_data)[4] = "Area"
head(census_data)
set_cancensus_api_key(CensusMapper_8f2843919778393ad76175db2790989e', install=TRUE)
library(cancensus)
all_regions = list_census_regions("CA21") # 2021 census
View(all_regions)
library(cancensus)
all_regions = list_census_regions("CA21") # 2021 census
set_cancensus_api_key(CensusMapper_8f2843919778393ad76175db2790989e', install=TRUE)
all_regions = list_census_regions("CA21") # 2021 census
ural_on_regions = subset(all_regions,
level=="CSD" &  # census subdivisions
PR_UID == 35 & # in Ontario
is.na(CMA_UID)) # not inside a metropolitan area
csd_list = list(CSD = rural_on_regions$region)
rural_on_regions = subset(all_regions,
level=="CSD" &  # census subdivisions
PR_UID == 35 & # in Ontario
is.na(CMA_UID)) # not inside a metropolitan area
csd_list = list(CSD = rural_on_regions$region)
census_data = get_census(dataset = "CA21",
regions = csd_list,
level = "CSD")
names(census_data)[4] = "Area"
head(census_data)
set_cancensus_api_key('CensusMapper_8f2843919778393ad76175db2790989e', install=TRUE)
set_cancensus_api_key('CensusMapper_8f2843919778393ad76175db2790989e', install=TRUE, overwrite=TRUE)
library(cancensus)
all_regions = list_census_regions("CA21") # 2021 census
rural_on_regions = subset(all_regions,
level=="CSD" &  # census subdivisions
PR_UID == 35 & # in Ontario
is.na(CMA_UID)) # not inside a metropolitan area
csd_list = list(CSD = rural_on_regions$region)
census_data = get_census(dataset = "CA21",
regions = csd_list,
level = "CSD")
names(census_data)[4] = "Area"
head(census_data)
all_regions = list_census_regions("CA21") # 2021 census
View(all_regions)
View(all_regions)
rural_on_regions = subset(all_regions,
level=="CSD" &  # census subdivisions
PR_UID == 35 & # in Ontario
is.na(CMA_UID)) # not inside a metropolitan area
csd_list = list(CSD = rural_on_regions$region)
census_data = get_census(dataset = "CA21",
regions = csd_list,
level = "CSD")
names(census_data)[4] = "Area"
head(census_data)
View(all_regions)
View(all_regions)
all_regions = list_census_regions("CA21") # 2021 census
View(all_regions)
