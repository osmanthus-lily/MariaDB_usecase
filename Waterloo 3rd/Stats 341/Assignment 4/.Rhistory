get_info <- function(df) {
c(mean = mean(df$citecount), var = var(df$citecount), unique = length(unique(df$citecount)))
}
s05_info <- get_info(df_S05)
s10_info <- get_info(df_S10)
s20_info <- get_info(df_S20)
s50_info <- get_info(df_S50)
kable(data.frame(
Dataset = c("Population", "Sample of Size 5", "Sample of Size 10", "Sample of Size 20", "Sample of Size 50"),
`Sample Size` = c(nrow(df_law), 5, 10, 20, 50),
`Mean Citecount` = c(pop_mean, s05_info[1], s10_info[1], s20_info[1], s50_info[1]),
`Variance Citecount` = c(pop_var, s05_info[2], s10_info[2], s20_info[2], s50_info[2]),
`Unique Citation Counts` = c(pop_unique, s05_info[3], s10_info[3], s20_info[3], s50_info[3])
))
#set.seed(341)
B=1000
S_idx = 1:5
n = length(S_idx)
means = rep(NA,B)
vars = rep(NA,B)
uniques_Ncite = rep(NA,B)
uniques_ID = rep(NA,B)
for (k in 1:B) {
Sstar_idx <- sample(S_idx, n, replace = TRUE)
samp <- df_S05[Sstar_idx,]
means[k] <- mean(samp$citecount)
vars[k] <- var(samp$citecount)
uniques_Ncite[k] <- length(unique(samp$citecount))
uniques_ID[k] <- length(unique(samp$ID))
}
par(mfrow = c(2, 2))
hist(means, main = "Resampled Means(n = 5)")
hist(vars, main = "Resampled Variances(n = 5)")
hist(uniques_Ncite, main = "Unique Citation Counts")
hist(uniques_ID, main = "Unique Legal Decision IDs")
B=1000
S_idx_50 = 1:50
n_50 = length(S_idx_50)
means_50 = rep(NA,B)
vars_50 = rep(NA,B)
uniques_Ncite_50 = rep(NA,B)
uniques_ID_50 = rep(NA,B)
for (k in 1:B) {
Sstar_idx_50 <- sample(S_idx_50, n_50, replace = TRUE)
samp_50 <- df_S50[Sstar_idx_50,]
means_50[k] <- mean(samp_50$citecount)
vars_50[k] <- var(samp_50$citecount)
uniques_Ncite_50[k] <- length(unique(samp_50$citecount))
uniques_ID_50[k] <- length(unique(samp_50$ID))
}
par(mfrow = c(2, 2))
hist(means_50, main = "Resampled Means(n = 50)")
hist(vars_50, main = "Resampled Variances(n = 50)")
hist(uniques_Ncite_50, main = "Unique Citation Counts")
hist(uniques_ID_50, main = "Unique Legal Decision IDs")
B=1000
D=100
S_idx = 1:5
n = length(S_idx)
means = rep(NA,B*D)
vars = rep(NA,B*D)
uniques_Ncite = rep(NA,B*D)
uniques_ID = rep(NA,B*D)
for (k in 1:B) {
Sstar_idx <- sample(S_idx, n, replace = TRUE)
for (l in 1:D) {
Sstarstar_idx <- sample(Sstar_idx, n, replace = TRUE)
samp <- df_S05[Sstarstar_idx, ]
i <- l + (k - 1) * D
means[i] <- mean(samp$citecount)
vars[i] <- var(samp$citecount)
uniques_Ncite[i] <- length(unique(samp$citecount))
uniques_ID[i] <- length(unique(samp$ID))
}
}
par(mfrow = c(2,2))
hist(means, main = "Double Resampled Means (n=5)")
hist(vars, main = "Double Resampled Variances (n=5)")
hist(uniques_Ncite, main = "Unique Citation Counts (Double)")
hist(uniques_ID, main = "Unique IDs (Double)")
B=1000
D=100
S_idx_50 = 1:50
n_50 = length(S_idx_50)
means_50 = rep(NA,B*D)
vars_50 = rep(NA,B*D)
uniques_Ncite_50 = rep(NA,B*D)
uniques_ID_50 = rep(NA,B*D)
for (k in 1:B) {
Sstar_idx_50 <- sample(S_idx_50, n_50, replace = TRUE)
for (l in 1:D) {
Sstarstar_idx_50 <- sample(Sstar_idx_50, n_50, replace = TRUE)
samp <- df_S50[Sstarstar_idx_50, ]
i <- l + (k - 1) * D
means_50[i] <- mean(samp$citecount)
vars_50[i] <- var(samp$citecount)
uniques_Ncite_50[i] <- length(unique(samp$citecount))
uniques_ID_50[i] <- length(unique(samp$ID))
}
}
par(mfrow = c(2,2))
hist(means_50, main = "Double Resampled Means (n=50)")
hist(vars_50, main = "Double Resampled Variances (n=50)")
hist(uniques_Ncite_50, main = "Unique Citation Counts (Double)")
hist(uniques_ID_50, main = "Unique IDs (Double)")
knitr::opts_chunk$set(echo = TRUE)
df_law = read.csv("Lawnodes.csv")
lm_model <- lm(df_law$citecount ~ df_law$$dissent)
summary(lm_model)
alpha_hat <- coef(lm_model)[1]
beta_hat <- coef(lm_model)[2]
ATE <- beta_hat
lift <- beta_hat / alpha_hat
lm_model <- lm(citecount ~ dissent, data = df_law)
summary(lm_model)
alpha_hat <- coef(lm_model)[1]
beta_hat <- coef(lm_model)[2]
ATE <- beta_hat
lift <- beta_hat / alpha_hat
lm_model <- lm(citecount ~ dissent, data = df_law)
summary(lm_model)
alpha_hat <- coef(lm_model)[1]
beta_hat <- coef(lm_model)[2]
ATE <- beta_hat
lift <- beta_hat / alpha_hat
ATE
lift
set.seed(123)
B <- 1000
boot_samples <- replicate(B, S(sample[1:nrow(S), replace = TRUE),], simplify = FALSE)
set.seed(123)
B <- 1000
boot_samples <- replicate(B, S[sample(1:nrow(S), replace = TRUE),], simplify = FALSE)
set.seed(123)
B <- 1000
n <- nrow(df_law)
bootstrap_samples <- vector("list", B)
for (b in 1:B) {
indices <- sample(1:n, size = n, replace=TRUE)
bootstrap_samples[[b]] <- df[indices, ]
}
set.seed(123)
B <- 1000
n <- nrow(df_law)
bootstrap_samples <- vector("list", B)
for (b in 1:B) {
indices <- sample(1:n, size = n, replace=TRUE)
bootstrap_samples[[b]] <- df_law[indices, ]
}
View(bootstrap_samples)
View(bootstrap_samples)
ATE_star <- numeric(B)
lift_star <- numeric(B)
for (b in 1:B) {
model_b <- lm(citecount ~ dissent, data =bootstrap_samples)
alpha_b <- coef(model_b)[1]
beta_b <- coef(model_b)[2]
ATE_star[b] <- beta_b
lift_star[b] <- beta_b / alpha_b
}
hist(ATE_star, main = "Bootstrap Distribution of ATE", xlab = "ATE", col = "lightblue")
abline(v = ATE, col = "red", lwd = 2)
hist(lift_star, main = "Bootstrap Distribution of Lift", xlab = "lift", col = "lightgreen")
abline(v = lift, col = "red", lwd = 2)
ATE_star
ATE_star <- numeric(B)
lift_star <- numeric(B)
for (b in 1:B) {
model_b <- lm(citecount ~ dissent, data =bootstrap_samples)
alpha_b <- coef(model_b)[1]
beta_b <- coef(model_b)[2]
ATE_star[b] <- beta_b
lift_star[b] <- beta_b / alpha_b
}
hist(ATE_star, main = "Bootstrap Distribution of ATE", xlab = "ATE", col = "lightblue")
abline(v = ATE, col = "red", lwd = 2)
hist(lift_star, main = "Bootstrap Distribution of Lift", xlab = "lift", col = "lightgreen")
abline(v = lift, col = "red", lwd = 2)
ATE_star <- numeric(B)
lift_star <- numeric(B)
for (b in 1:B) {
model_b <- lm(citecount ~ dissent, data = bootstrap_samples[[b]])
alpha_b <- coef(model_b)[1]
beta_b <- coef(model_b)[2]
ATE_star[b] <- beta_b
lift_star[b] <- beta_b / alpha_b
}
hist(ATE_star, main = "Bootstrap Distribution of ATE", xlab = "ATE", col = "lightblue")
abline(v = ATE, col = "red", lwd = 2)
hist(lift_star, main = "Bootstrap Distribution of Lift", xlab = "lift", col = "lightgreen")
abline(v = lift, col = "red", lwd = 2)
ATE_se <- sd(ATE_star)
lift_se <- sd(lift_star)
ATE_CI <- c(ATE - 1.96 * ATE_se, ATE + 1.96 * ATE_se)
lift_CI <- c(lift - 1.96 * lift_se, lift + 1.96 * lift_se)
print(ATE_CI)
print(lift_CI)
ATE_CI_percentile <- quantile(ATE_star, probs = c(0.025, 0.975))
lift_CI_percentile <- quantile(lift_star, probs = c(0.025, 0.975))
print(ATE_CI_percentile)
print(lift_CI_percentile)
bootstrap_t_interval_new <- function(S, a, confidence, B, D) {
Pstar <- S
aPstar <- a(S)  # Attribute value on original sample
n <- length(S)
# Outer bootstrap: compute a(S*) and its z-score
bvals <- sapply(1:B, FUN = function(b) {
Sstar <- sample(Pstar, size = n, replace = TRUE)
aSstar <- a(Sstar)
# Inner bootstrap: estimate SD of a(S*)
Pstarstar <- Sstar
SD_aSstar <- sd(sapply(1:D, FUN = function(d) {
Sstarstar <- sample(Pstarstar, size = n, replace = TRUE)
a(Sstarstar)
}))
z <- (aSstar - aPstar) / SD_aSstar
c(aSstar, z)
})
# Extract values
SDhat <- sd(bvals[1, ])     # Standard deviation of aSstar values
zVals <- bvals[2, ]         # z-scores
# Quantile cutoffs
alpha <- 1 - confidence
cValues <- quantile(zVals, probs = c((1 - confidence)/2,(1 + confidence) / 2), na.rm = TRUE)
cLower <- min(cValues)
cUpper <- max(cValues)
# Compute interval
interval <- c(
lower = aPstar - cUpper * SDhat,
middle = aPstar,
upper = aPstar - cLower * SDhat
)
return(interval)
}
ATE_func <- function(data) {
model <- lm(citecount ~ dissent, data = data)
coef(model)[2]
}
lift_func <- function(data) {
model <- lm(citecount ~ dissent, data = data)
beta <- coef(model)[2]
alpha <- coef(model)[1]
beta / alpha
}
B_small <- 100
D <- 100
ATE_boot_t_CI <- bootstrap_t_interval_new(df_law, ATE_func, B = B_small, D = D)
lift_boot_t_CI <- bootstrap_t_interval_new(df_law, lift_func, B = B_small, D = D)
print(ATE_boot_t_CI)
print(lift_boot_t_CI)
ATE_func <- function(data) {
model <- lm(citecount ~ dissent, data = data)
coef(model)[2]
}
lift_func <- function(data) {
model <- lm(citecount ~ dissent, data = data)
beta <- coef(model)[2]
alpha <- coef(model)[1]
beta / alpha
}
B_small <- 100
D <- 100
ATE_boot_t_CI <- bootstrap_t_interval_new(df_law, ATE_func, confidence = 0.95, B = B_small, D = D)
lift_boot_t_CI <- bootstrap_t_interval_new(df_law, lift_func, confidence = 0.95, B = B_small, D = D)
print(ATE_boot_t_CI)
print(lift_boot_t_CI)
bootstrap_t_interval_new <- function(S, a, confidence, B, D) {
Pstar <- S
aPstar <- a(S)  # Attribute value on original sample
n <- length(S)
# Outer bootstrap: compute a(S*) and its z-score
bvals <- sapply(1:B, FUN = function(b) {
indices <- sample(1:n, size = n, replace = TRUE)
Sstar <- Pstar[indicies,]
aSstar <- a(Sstar)
# Inner bootstrap: estimate SD of a(S*)
Pstarstar <- Sstar
SD_aSstar <- sd(sapply(1:D, FUN = function(d) {
inner_indices <- sample(1:n, size = n, replace = TRUE)
Sstarstar <- Sstar[inner_indices]
a(Sstarstar)
}))
z <- (aSstar - aPstar) / SD_aSstar
c(aSstar, z)
})
# Extract values
SDhat <- sd(bvals[1, ])     # Standard deviation of aSstar values
zVals <- bvals[2, ]         # z-scores
# Quantile cutoffs
alpha <- 1 - confidence
cValues <- quantile(zVals, probs = c((1 - confidence)/2,(1 + confidence) / 2), na.rm = TRUE)
cLower <- min(cValues)
cUpper <- max(cValues)
# Compute interval
interval <- c(
lower = aPstar - cUpper * SDhat,
middle = aPstar,
upper = aPstar - cLower * SDhat
)
return(interval)
}
ATE_func <- function(data) {
model <- lm(citecount ~ dissent, data = data)
coef(model)[2]
}
lift_func <- function(data) {
model <- lm(citecount ~ dissent, data = data)
beta <- coef(model)[2]
alpha <- coef(model)[1]
beta / alpha
}
B_small <- 100
D <- 100
ATE_boot_t_CI <- bootstrap_t_interval_new(df_law, ATE_func, confidence = 0.95, B = B_small, D = D)
lift_boot_t_CI <- bootstrap_t_interval_new(df_law, lift_func, confidence = 0.95, B = B_small, D = D)
print(ATE_boot_t_CI)
print(lift_boot_t_CI)
bootstrap_t_interval_new <- function(S, a, confidence, B, D) {
Pstar <- S
aPstar <- a(S)  # Attribute value on original sample
n <- length(S)
# Outer bootstrap: compute a(S*) and its z-score
bvals <- sapply(1:B, FUN = function(b) {
indices <- sample(1:n, size = n, replace = TRUE)
Sstar <- Pstar[indices,]
aSstar <- a(Sstar)
# Inner bootstrap: estimate SD of a(S*)
Pstarstar <- Sstar
SD_aSstar <- sd(sapply(1:D, FUN = function(d) {
inner_indices <- sample(1:n, size = n, replace = TRUE)
Sstarstar <- Sstar[inner_indices,]
a(Sstarstar)
}))
z <- (aSstar - aPstar) / SD_aSstar
c(aSstar, z)
})
# Extract values
SDhat <- sd(bvals[1, ])     # Standard deviation of aSstar values
zVals <- bvals[2, ]         # z-scores
# Quantile cutoffs
alpha <- 1 - confidence
cValues <- quantile(zVals, probs = c((1 - confidence)/2,(1 + confidence) / 2), na.rm = TRUE)
cLower <- min(cValues)
cUpper <- max(cValues)
# Compute interval
interval <- c(
lower = aPstar - cUpper * SDhat,
middle = aPstar,
upper = aPstar - cLower * SDhat
)
return(interval)
}
ATE_func <- function(data) {
model <- lm(citecount ~ dissent, data = data)
coef(model)[2]
}
lift_func <- function(data) {
model <- lm(citecount ~ dissent, data = data)
beta <- coef(model)[2]
alpha <- coef(model)[1]
beta / alpha
}
B_small <- 100
D <- 100
ATE_boot_t_CI <- bootstrap_t_interval_new(df_law, ATE_func, confidence = 0.95, B = B_small, D = D)
lift_boot_t_CI <- bootstrap_t_interval_new(df_law, lift_func, confidence = 0.95, B = B_small, D = D)
print(ATE_boot_t_CI)
print(lift_boot_t_CI)
knitr::opts_chunk$set(echo = TRUE)
df_law = read.csv("Lawnodes.csv")
lm_model <- lm(citecount ~ dissent, data = df_law)
summary(lm_model)
alpha_hat <- coef(lm_model)[1]
beta_hat <- coef(lm_model)[2]
ATE <- beta_hat
lift <- beta_hat / alpha_hat
ATE
lift
set.seed(123)
B <- 1000
n <- nrow(df_law)
bootstrap_samples <- vector("list", B)
for (b in 1:B) {
indices <- sample(1:n, size = n, replace=TRUE)
bootstrap_samples[[b]] <- df_law[indices, ]
}
ATE_star <- numeric(B)
lift_star <- numeric(B)
for (b in 1:B) {
model_b <- lm(citecount ~ dissent, data = bootstrap_samples[[b]])
alpha_b <- coef(model_b)[1]
beta_b <- coef(model_b)[2]
ATE_star[b] <- beta_b
lift_star[b] <- beta_b / alpha_b
}
hist(ATE_star, main = "Bootstrap Distribution of ATE", xlab = "ATE", col = "lightblue")
abline(v = ATE, col = "red", lwd = 2)
hist(lift_star, main = "Bootstrap Distribution of Lift", xlab = "lift", col = "lightgreen")
abline(v = lift, col = "red", lwd = 2)
ATE_se <- sd(ATE_star)
lift_se <- sd(lift_star)
ATE_CI <- c(ATE - 1.96 * ATE_se, ATE + 1.96 * ATE_se)
lift_CI <- c(lift - 1.96 * lift_se, lift + 1.96 * lift_se)
print(ATE_CI)
print(lift_CI)
ATE_CI_percentile <- quantile(ATE_star, probs = c(0.025, 0.975))
lift_CI_percentile <- quantile(lift_star, probs = c(0.025, 0.975))
print(ATE_CI_percentile)
print(lift_CI_percentile)
bootstrap_t_interval_new <- function(S, a, confidence, B, D) {
Pstar <- S
aPstar <- a(S)  # Attribute value on original sample
n <- length(S)
# Outer bootstrap: compute a(S*) and its z-score
bvals <- sapply(1:B, FUN = function(b) {
indices <- sample(1:n, size = n, replace = TRUE)
Sstar <- Pstar[indices,]
aSstar <- a(Sstar)
# Inner bootstrap: estimate SD of a(S*)
Pstarstar <- Sstar
SD_aSstar <- sd(sapply(1:D, FUN = function(d) {
inner_indices <- sample(1:n, size = n, replace = TRUE)
Sstarstar <- Sstar[inner_indices,]
a(Sstarstar)
}))
z <- (aSstar - aPstar) / SD_aSstar
c(aSstar, z)
})
# Extract values
SDhat <- sd(bvals[1, ])     # Standard deviation of aSstar values
zVals <- bvals[2, ]         # z-scores
# Quantile cutoffs
alpha <- 1 - confidence
cValues <- quantile(zVals, probs = c((1 - confidence) / 2,(1 + confidence) / 2), na.rm = TRUE)
cLower <- min(cValues)
cUpper <- max(cValues)
# Compute interval
interval <- c(
lower = aPstar - cUpper * SDhat,
middle = aPstar,
upper = aPstar - cLower * SDhat
)
return(interval)
}
ATE_func <- function(data) {
model <- lm(citecount ~ dissent, data = data)
coef(model)[2]
}
lift_func <- function(data) {
model <- lm(citecount ~ dissent, data = data)
beta <- coef(model)[2]
alpha <- coef(model)[1]
beta / alpha
}
B_small <- 100
D <- 100
ATE_boot_t_CI <- bootstrap_t_interval_new(df_law, ATE_func, confidence = 0.95, B = B_small, D = D)
lift_boot_t_CI <- bootstrap_t_interval_new(df_law, lift_func, confidence = 0.95, B = B_small, D = D)
print(ATE_boot_t_CI)
print(lift_boot_t_CI)
bootstrap_t_interval_new <- function(S, a, confidence, B, D) {
Pstar <- S
aPstar <- a(S)  # Attribute value on original sample
n <- nrow(S)
# Outer bootstrap: compute a(S*) and its z-score
bvals <- sapply(1:B, FUN = function(b) {
indices <- sample(1:n, size = n, replace = TRUE)
Sstar <- Pstar[indices,]
aSstar <- a(Sstar)
# Inner bootstrap: estimate SD of a(S*)
Pstarstar <- Sstar
inner_n <- nrow(Sstar)
SD_aSstar <- sd(sapply(1:D, FUN = function(d) {
inner_indices <- sample(1:inner_n, size = inner_n, replace = TRUE)
Sstarstar <- Sstar[inner_indices,]
a(Sstarstar)
}))
z <- (aSstar - aPstar) / SD_aSstar
c(aSstar, z)
})
# Extract values
SDhat <- sd(bvals[1, ])     # Standard deviation of aSstar values
zVals <- bvals[2, ]         # z-scores
# Quantile cutoffs
alpha <- 1 - confidence
cValues <- quantile(zVals, probs = c((1 - confidence) / 2,(1 + confidence) / 2), na.rm = TRUE)
cLower <- min(cValues)
cUpper <- max(cValues)
# Compute interval
interval <- c(
lower = aPstar - cUpper * SDhat,
middle = aPstar,
upper = aPstar - cLower * SDhat
)
return(interval)
}
ATE_func <- function(data) {
model <- lm(citecount ~ dissent, data = data)
coef(model)[2]
}
lift_func <- function(data) {
model <- lm(citecount ~ dissent, data = data)
beta <- coef(model)[2]
alpha <- coef(model)[1]
beta / alpha
}
B_small <- 100
D <- 100
ATE_boot_t_CI <- bootstrap_t_interval_new(df_law, ATE_func, confidence = 0.95, B = B_small, D = D)
lift_boot_t_CI <- bootstrap_t_interval_new(df_law, lift_func, confidence = 0.95, B = B_small, D = D)
print(ATE_boot_t_CI)
print(lift_boot_t_CI)
